<%= turbo_frame_tag @likeable.class.name.downcase + "-likes" do %>
  <%= button_to "", likes_path(likeable_type: @likeable.class.name, likeable_id: @likeable.id), class: @likeable.class.name.downcase + "-actions-like" %>
<% end %>